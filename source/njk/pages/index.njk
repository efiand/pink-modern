{% extends "layout.njk" %}

{% block main %}
  <header class="promo page-main__promo">
    <h1 class="visually-hidden">Pink.</h1>

    <b class="promo__lead">Взгляните на жизнь иначе!</b>

    <a class="promo__button" href="#!">Скачать приложение</a>

    {% set mix = 'promo__markets' %}
    {% include 'blocks/markets.njk' %}

    <p class="promo__description">Доступно для iPhone, iPad, Android, Windows Phone, OS X, Windows 8</p>
  </header>

  <section class="features page-main__features">
    <h2 class="visually-hidden">Особенности</h2>

    <b class="features__lead">Устали от серости мегаполиса?</b>
    <b class="features__lead">Нам есть, что вам предложить!</b>

    <dl class="features__list">
      <dt class="features__item">Поднимает настроение</dt>
      <dd class="features__description">Приложение позволит вам победить осеннюю хандру и депрессию буквально в несколько кликов!</dd>
      <dt class="features__item">Меняет мир вокруг</dt>
      <dd class="features__description">Сделайте снимок и украсьте его смайликом или текстовой подписью, чтобы усилить эффект</dd>
      <dt class="features__item">Заводит новых друзей</dt>
      <dd class="features__description">Ставьте лайки, комментируйте, делитесь фотографиями с друзьями и заводите новых</dd>
    </dl>
  </section>

  <section class="reviews page-main__reviews">
    <h2 class="visually-hidden">Отзывы</h2>

    <ul class="reviews__list">
      {% for review in data.reviews %}
        <li class="reviews__item" data-slide="reviews-{{ loop.index }}">
          <blockquote class="reviews__body">
            <cite class="reviews__author">{{ review.author }}</cite>
            <p class="reviews__text">{{ review.text }}</p>
          </blockquote>
        </li>
      {% endfor %}
    </ul>

    {% set togglers = {
      mix: 'reviews__togglers',
      id: 'reviews',
      title: 'Отзыв',
      length: data.reviews | length
    } %}
    {% include 'blocks/togglers.njk' %}
  </section>

  {% set mix = 'page-main__tariffs' %}
  {% include 'blocks/tariffs.njk' %}

  <footer class="contacts page-main__footer">
    <p class="contacts__heading">Остались вопросы?</p>
    <p class="contacts__heading">Свяжитесь с нами!</p>

    <dl class="contacts__list">
      <dt class="contacts__item">Звоните:</dt>
      <dd class="contacts__description">
        <a href="contacts__link" href="tel:+78125556666">+7 (812) 555-66-66</a>
      </dd>
      <dt class="contacts__item">Пишите:</dt>
      <dd class="contacts__description">
        <a href="contacts__link" href="mailto:mail@htmlacademy.ru">mail@htmlacademy.ru</a>
      </dd>
      <dt class="contacts__item">Приезжайте в гости:</dt>
      <dd class="contacts__description">
        <a href="contacts__link" href="#!">ул. Большая Конюшенная, 19/8</a>
      </dd>
    </dl>
  </footer>
{% endblock %}
